function insertionSort(arr){
    for(let i = 0; i < arr.length; i++){
        let numToInsert = arr[i];
    //     let j = i-1;
    //     while(j >= 0 && numToInsert < arr[j]){
    //         arr[j + 1] = numToInsert;
    //         j = j - 1;
    //     }
    //     arr[j + 1] = arr[i];
    // }
    // return arr;
}

// Time C: Ω(n) O(n^2) | Space C: O(1)

// got time complexity wrong